import { FiltroMisCursosPipe } from './filtro-mis-cursos.pipe';

describe('FiltroMisCursosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroMisCursosPipe();
    expect(pipe).toBeTruthy();
  });
});
